PAMSParagon Apartment Management SystemFront Desk Implementation — Git Merge GuideModuleAdvanced Software Development — UFCF8S-30-2GroupGroup 6AuthorTerrence  (Front Desk Role)Base BranchASD_PAMS-alpha-tomi-bethel-combo  (the COMBO zip)Source BranchASD_PAMS-Alpha-B  (the ZP zip — contains Front Desk work)PurposeStep-by-step guide to merge the Front Desk implementation into the COMBO codebaseDateApril 2026Before You Start — Read ThisThe COMBO zip is the most up-to-date shared codebase (Alpha + Tomisin + Bethel). The ZP zip is Terrence's branch with the working Front Desk. This guide tells you exactly what to copy from the ZP zip into the COMBO zip to get Front Desk working without breaking anything else.01  Overview of ChangesWhat Was Added (Front Desk Features)The following features are implemented in the ZP zip and need to be merged into the COMBO zip:FeatureWhat It DoesRequirementTenant Registration FormFull form: NI, name, email, phone, occupation, references, emergency contactFR-2.110-Minute Reservation TimerLocks an apartment slot while staff fills the registration form. Released if time expires or cancelledNFR-3Lease Assignment DialogAssigns any registered tenant to any available apartment with selectable dates and rentFR-2.2Active Lease ViewerShows all active leases; supports early-leave termination with 5% penaltyFR-2.3Maintenance Request LoggingFront Desk logs issues on behalf of tenants — goes straight to Maintenance Staff queueFR-4.1Complaint LoggingRecords tenant complaints as maintenance tickets with [COMPLAINT] tag and priority selectionFR-2.xTenant SearchSearches by name, NI, email, or phone — shows current lease and apartment statusFR-2.1Sidebar Navigation (all buttons)All sidebar buttons now navigate to real sub-pages (previously placeholders)FR-2.xFiles Changed — Quick ReferenceOnly these files need to be touched in the COMBO codebase. Everything else stays the same.FileChange TypeSummarypages/frontdesk_page.pyFULL REPLACEOld file was a stub (236 lines). Replace with the full 1014-line ZP versiondatabase/db_service.pyADD FUNCTIONS3 new functions added at the bottom of the filecomponents/sidebar.py1-LINE EDITFront Desk nav items updated to match new sub-pagesdatabase/schema.pyADD TABLENew apartment_reservations table needed for the timer featureImportant — Files NOT to touchapp.py, admin_page.py, finance_page.py, seed.py, mock_data.py, requirements.txt, shared_dialogs.py, models.py — these are IDENTICAL between both zips. Do not overwrite them.02  Step 1 — database/db_service.pyWhat to AddOpen database/db_service.py in the COMBO codebase. Scroll to the very end of the file. Paste the following three functions after the last existing function.Why these functions?The frontdesk_page.py imports create_apartment_reservation, release_apartment_reservation, and get_complaints directly from db_service. Without these three functions, the app will crash on import with a ImportError before it even opens.Function 1 — create_apartment_reservationPurpose: Called when a Front Desk member clicks 'Lock & Start Timer' on the lease dialog. It inserts a row into the apartment_reservations table AND flips the apartment status to 'reserved_pending' so no other staff member can grab the same room.def create_apartment_reservation(apt_id: str, user_session_id: str,                                 expires_at) -> str | None:    rid = _new_id()    try:        with get_db() as conn:            conn.execute(                """INSERT INTO apartment_reservations                   (reservation_id, apt_id, user_session_id, expires_at, status)                   VALUES (?,?,?,?,?)""",                (rid, apt_id, user_session_id, expires_at, "active")            )            conn.execute(                "UPDATE apartments SET status = 'reserved_pending' WHERE apt_id = ?",                (apt_id,)            )        return rid    except Exception:        return NoneFunction 2 — release_apartment_reservationPurpose: Called when the 10-minute timer expires, or when the staff member clicks Cancel. Deletes the reservation row from the table and flips the apartment back to 'available'.def release_apartment_reservation(reservation_id: str) -> bool:    try:        with get_db() as conn:            row = conn.execute(                "SELECT apt_id FROM apartment_reservations WHERE reservation_id = ?",                (reservation_id,)            ).fetchone()            if not row: return False            apt_id = row[0]            conn.execute(                "DELETE FROM apartment_reservations WHERE reservation_id = ?",                (reservation_id,)            )            conn.execute(                "UPDATE apartments SET status = 'available' WHERE apt_id = ?",                (apt_id,)            )            return True    except Exception:        return FalseFunction 3 — get_complaintsPurpose: Retrieves all maintenance tickets that were logged as complaints. Complaints are identified by the [COMPLAINT] tag in their description text. This is used by the Front Desk dashboard to show the 'Active Complaints' stat card.def get_complaints() -> list[dict]:    with get_db() as conn:        rows = conn.execute(            "SELECT * FROM maintenance_tickets WHERE description LIKE '%[COMPLAINT]%'             ORDER BY created_at DESC"        ).fetchall()    return _rows_to_dicts(rows)03  Step 2 — database/schema.pyWhat to AddOpen database/schema.py. Find the CREATE_TABLES_SQL list. Add the following table definition as a new entry at the end of the list (before the closing ] bracket).Why is this needed?The create_apartment_reservation function (from Step 1) does an INSERT INTO apartment_reservations. If this table doesn't exist in the schema, the app will throw an OperationalError: no such table: apartment_reservations the first time a reservation is attempted. Adding it to the schema means init_db() creates it automatically on startup.New Table — apartment_reservations# ── apartment_reservations (Front Desk timer feature — added by Terrence) ──"""CREATE TABLE IF NOT EXISTS apartment_reservations (    reservation_id  TEXT PRIMARY KEY,    apt_id          TEXT NOT NULL REFERENCES apartments(apt_id),    user_session_id TEXT NOT NULL,    expires_at      TIMESTAMP NOT NULL,    status          TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active', 'expired')),    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP);""",📍  Where to insert: This should be the last entry inside CREATE_TABLES_SQL = [ ... ]. Add a comma after the preceding table's closing triple-quote, then paste this block.05  Step 3 — pages/frontdesk_page.pyWhat to DoThis is the main step. The COMBO version has a stub frontdesk_page.py (236 lines) with no real functionality. The ZP version has the full implementation (1014 lines).Action: Full File ReplaceDelete the entire contents of pages/frontdesk_page.py in the COMBO codebase. Copy and paste the entire contents of pages/frontdesk_page.py from the ZP zip in its place.What Is Inside the New frontdesk_page.pyHere is a breakdown of every class and major method, so teammates can understand the code quickly:Imports (lines 1–35)The new file imports everything it needs from db_service directly. It does not use mock_data for the main data — it reads from the real SQLite database. The key imports are:get_tenants, register_tenant — for reading and creating tenant recordsget_apartments, get_leases, get_cities — for populating dropdownscreate_lease — for assigning a tenant to an apartmentcreate_apartment_reservation, release_apartment_reservation — for the 10-minute timerlog_maintenance_request — for logging issues on behalf of tenantsget_maintenance_tickets — for showing open tickets on the dashboardClass: LeaseManagementDialog (lines 36–204)A QDialog that handles assigning a tenant to an apartment. Contains the 10-minute countdown timer.MethodWhat It Does__init__Builds the form: apartment dropdown, tenant dropdown, start/end date pickers, rent spinner. Also starts the QTimer._update_timerFires every second. Decrements the counter. If it reaches 0, calls release_apartment_reservation() and closes the dialog with an error message.reject (override)Called on Cancel click. Stops the timer and releases the reservation before closing — so the apartment goes back to 'available' immediately._assign_leaseValidates the form. Calls create_lease() in db_service which creates the lease record AND sets the apartment to 'occupied' in one transaction.Class: ActiveLeaseView (lines 206–364)A QDialog that shows all active leases. Allows Front Desk to process an early-leave request (calls process_early_leave() from db_service which applies the 5% monthly rent penalty automatically).Class: FrontDeskPage (lines 367–624)The main dashboard widget. Index 3 in the MainApp stacked widget. This is what the staff member sees when they log in.MethodWhat It Does__init__Builds the sidebar and the scrollable dashboard content area. Loads live data on construction._build_headerTitle bar at the top of the page._build_stat_cardsFour stat cards: Active Tenants, New This Month, Pending Requests, Active Complaints. All pulled from the live database._build_quick_actionsFour clickable action cards wired to real dialogs: Register Tenant, Maintenance Request, Log Complaint, Tenant Inquiry._build_recent_tenants_tableShows the 10 most recently registered tenants from the DB._build_maintenance_tableShows open maintenance tickets (status not Completed/Closed)._open_tenant_registrationOpens RegisterTenantDialog. On success, refreshes the dashboard._open_tenant_searchOpens TenantSearchDialog._open_maintenance_dialogOpens MaintenanceRequestDialog._open_complaint_dialogOpens ComplaintDialog._on_page_changedReceives sidebar button signals and routes to the correct dialog/view._logoutCalls main_app.logout() to return to the login screen.Class: RegisterTenantDialog (lines 728–795)A QDialog with a full tenant registration form. Fields: First Name, Last Name, NI Number, Email, Phone, Emergency Contact, Occupation. On submit it calls register_tenant() from db_service. If successful it then opens the LeaseManagementDialog automatically so the staff member can immediately assign an apartment.Class: TenantSearchDialog (lines 800–862)A QDialog with a search bar and results table. Searches by name, NI, email, or phone using get_tenants() and filters the results client-side. Shows lease status and apartment assignment for each result.Class: MaintenanceRequestDialog (lines 868–937)A QDialog for logging a maintenance issue. Fields: Apartment (dropdown), Description, Priority (Low/Medium/High). Calls log_maintenance_request() from db_service. The ticket appears in the Maintenance Staff queue with status='reported'.Class: ComplaintDialog (lines 943–1014)A QDialog for logging a tenant complaint. Prefixes the description with [COMPLAINT] before saving. This tag is how get_complaints() (in db_service) distinguishes complaints from regular maintenance tickets. Priority defaults to 'High' since complaints are treated as urgent.06  Structural Differences to Be Aware OfWhat Changed Between the Two Zips (Beyond Front Desk)During the diff analysis, several differences were found between the two zips that are NOT related to the Front Desk feature. These are changes Tomisin made in the COMBO version that are MORE advanced than what is in the ZP zip. The COMBO version is the correct, up-to-date one for these. Do NOT overwrite these with the ZP versions.1. database/db_service.py — Functions in COMBO that are NOT in ZPThese are Tomisin's Maintenance Staff additions. They exist in COMBO but not in ZP. They must be kept.FunctionPurpose (Keep in COMBO)assign_ticket()Assigns a maintenance ticket to a specific worker. Used by Maintenance Staff page.get_worker_availability()Returns active ticket counts per maintenance worker so the team knows who is free.get_apartment_capacity()Returns max tenants for a room type. Used in capacity-aware lease validation.get_equipment()Reads the equipment inventory table. Used in Maintenance Staff equipment tracking.update_equipment_stock()Updates quantity/status of a piece of equipment.add_equipment()Adds a new item to the equipment table.get_maintenance_financial_summary()Detailed cost breakdown for the Maintenance Staff dashboard.2. database/schema.py — Equipment Table (COMBO only)The COMBO schema has an 11th table: equipment. This supports Tomisin's equipment inventory feature on the Maintenance Staff page. The ZP zip does not have this table. When merging, keep the equipment table in the COMBO schema — do not replace the whole schema file with the ZP version.Merge approach for schema.pyDo NOT replace schema.py entirely. Instead, only ADD the apartment_reservations table (from Step 2 of this guide) as a new entry at the end of CREATE_TABLES_SQL. Leave everything else in the COMBO schema.py untouched.3. database/db_service.py — create_lease() DifferencesThe COMBO version of create_lease() has more advanced capacity checking than the ZP version. The COMBO version:Calls get_apartment_capacity() to find out how many tenants a room type can holdBlocks lease creation if the apartment is already at full capacityAutomatically sets apartment status to 'occupied' OR keeps it 'available' based on remaining capacity (useful for group houses and shared apartments)The ZP version always sets status to 'occupied' regardless of capacity. The COMBO version is more correct. Keep the COMBO version of create_lease().4. database/db_service.py — get_dashboard_stats() for Maintenance RoleThe COMBO version has an extra block in get_dashboard_stats() that handles the 'Maintenance Staff' role. It calculates live active requests, completed this month, average resolution time, and total maintenance costs. The ZP version is missing this block. Keep the COMBO version.5. pages/maintenance_page.py — COMBO is Much More CompleteThe COMBO maintenance_page.py has over 380 lines of Tomisin's full implementation (ticket assignment, worker availability, equipment tracking, cost reporting, full lifecycle management). The ZP version has only the basic stub. Do NOT overwrite maintenance_page.py — it only needs to be replaced if you are pulling Tomisin's version into a fresh codebase.6. pages/manager_page.py — COMBO has Extra FeaturesThe COMBO manager_page.py has two extra features compared to ZP:Staff Overview Page: A full staff directory table filterable by city, showing all users and their active/inactive statusDetailed Apartment Occupancy Table: Shows per-apartment capacity, active leases, spaces left, and current occupants under the occupancy reportThese features use get_users() and get_occupancy_report() with the extra capacity fields added by the COMBO db_service. Keep the COMBO manager_page.py.07  Git Merge WorkflowRecommended Git StepsFollow this order to safely merge the Front Desk work into the shared repository without conflicts.Step A — Make sure you are on the right branchgit checkout main          # or whatever your shared branch is calledgit pull origin main       # get the latest from everyoneStep B — Create a feature branch for the mergegit checkout -b feature/frontdesk-integrationStep C — Make the four changes described in Sections 02–05Edit only these files (as described in each section):database/db_service.py  — Add the 3 new functions at the bottomdatabase/schema.py       — Add the apartment_reservations tablecomponents/sidebar.py    — Update the Front Desk nav itemspages/frontdesk_page.py  — Replace entirely with the ZP versionStep D — Verify the app runspip install PyQt5python app.pyLog in as frontdesk / Front@123. Check that all sidebar buttons work, the registration form opens, and the timer starts when you click Lock.Step E — Commit and pushgit add pages/frontdesk_page.pygit add database/db_service.pygit add database/schema.pygit add components/sidebar.pygit commit -m "feat: add front desk implementation (FR-2.x, NFR-3)- Tenant registration form with NI, email, phone, occupation,  references, emergency contact (FR-2.1)- 10-minute apartment reservation timer (NFR-3)- Lease assignment dialog with tenant & apartment dropdowns (FR-2.2)- Active lease viewer with early-leave / penalty processing (FR-2.3)- Maintenance request and complaint logging (FR-4.1)- Tenant search by name, NI, email, phone- All sidebar nav buttons now functional- Added apartment_reservations table to schema- Added create/release reservation + get_complaints to db_service"git push origin feature/frontdesk-integrationStep F — Open a Pull RequestOn GitHub (or wherever your repo is hosted), open a PR from feature/frontdesk-integration into main. Tag all group members to review. The PR description can reference this guide.08  Testing the Front Desk ImplementationLogin CredentialsUsernamePasswordRolefrontdesk_brisFront@123Front-Desk Staff (Bristol branch)admin_bristolAdmin@123Administratorfrontdesk (mock)front123Front-Desk Staff (mock data fallback)Test ChecklistTenant Registration + TimerLog in as Front DeskClick 'Register Tenant' in Quick Actions or sidebarSelect an available apartment from the dropdownClick 'Lock & Start Timer' — the timer banner should appear with a 10:00 countdownCheck the database: the apartment's status should now be 'reserved_pending'Fill in all required fields (First Name, Last Name, NI Number, Email)Click 'Register Tenant' — success message should appearCheck the database: a new tenant row and a new lease row should exist; apartment should be 'occupied'Test expiry: Open the form, lock a room, then wait 10 minutes (or temporarily change RESERVATION_TIMEOUT to 10 seconds for testing). The room should revert to 'available' and a warning should appear.Lease AssignmentClick 'Assign Lease' in sidebarSelect an existing tenant and an available apartmentSet valid start and end datesClick 'Create Lease' — the apartment status should change to 'occupied'Maintenance RequestClick 'Maintenance Request' in sidebarSelect an apartment, enter a description, set prioritySubmit — verify the ticket appears in the 'Pending Maintenance Requests' table on the dashboardLog in as Maintenance Staff and verify the ticket appears in their queueComplaint LoggingClick 'Log Complaint' in sidebarEnter a complaint description and submitThe complaint should appear in the 'Active Complaints' stat card countIn the database, the ticket description should start with [COMPLAINT]Tenant SearchClick 'Tenant Inquiry' in sidebarSearch by partial name (e.g. 'smi' to find 'Smith')Verify results show lease status and apartment assignment09  TroubleshootingCommon Issues After MergingError / SymptomFixImportError: cannot import name 'create_apartment_reservation' from 'database.db_service'You forgot Step 1. Add the three new functions to db_service.py.OperationalError: no such table: apartment_reservationsYou forgot Step 2. Add the table to schema.py. Then delete data/pams.db and restart the app so init_db() recreates the database.Sidebar buttons do nothing when clickedYou forgot Step 3. Update the sidebar.py nav items to the new names.AttributeError: 'FrontDeskPage' object has no attribute '_open_tenant_registration'You still have the old stub frontdesk_page.py. Do a full file replace with the ZP version (Step 4).Apartment shows as 'available' even after lockingThe create_apartment_reservation function was not added correctly. Check it sets status = 'reserved_pending' in the UPDATE statement.Timer does not count down / stays at 10:00Check that QTimer from PyQt5.QtCore is imported. Also check self.timer.start(1000) is called in __init__ of LeaseManagementDialog.App crashes on startup with TypeError in mock_data referencesThe new frontdesk_page.py uses db_service directly, not mock_data. Make sure the database has been seeded — run python database/seed.py once.Missing maintenance staff features after mergeYou accidentally overwrote maintenance_page.py with the ZP version. Restore it from the COMBO zip — it is the correct, more complete version.10  Summary — Everything at a Glance#FileActionDetail1database/db_service.pyADD (3 functions)create_apartment_reservation, release_apartment_reservation, get_complaints — paste at end of file2database/schema.pyADD (1 table)apartment_reservations table — add as new entry in CREATE_TABLES_SQL list3components/sidebar.pyEDIT (1 block)Replace old Front-Desk Staff nav items with the 7 new button names4pages/frontdesk_page.pyFULL REPLACEDelete the 236-line stub; replace with the 1014-line ZP version in full—ALL OTHER FILESDO NOT TOUCHapp.py, admin_page.py, finance_page.py, maintenance_page.py, manager_page.py, seed.py, shared_dialogs.py are all identical or COMBO has the better versionFinal Reminder — Delete the database before first run after mergeAfter completing all four steps, delete data/pams.db (and data/pams.db-shm, data/pams.db-wal if present). When you run python app.py, init_db() will recreate the database with all tables including the new apartment_reservations table. Then run python database/seed.py to repopulate with test data.PAMS Group 6 — Advanced Software Development — UFCF8S-30-2